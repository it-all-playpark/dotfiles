return {
  "LazyVim/LazyVim",
  opts = {
    colorscheme = "catppuccin",
    -- Force telescope as the default picker, disable fzf
    defaults = {
      -- Use telescope for all picker operations
      picker = "telescope",
    },
  },
}
